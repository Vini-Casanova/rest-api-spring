CREATE TABLE `person` (
                          `id` bigint NOT NULL AUTO_INCREMENT,
                          `address` varchar(80) NOT NULL,
                          `name` varchar(80) NOT NULL,
                          PRIMARY KEY (`id`)
);